{
  "name": "manuscripts",
  "version": "1.0.0",
  "description": "manuscripts app",
  "main": "index.js",
  "repository": "git@github.com:khirayama/manuscripts.git",
  "author": "khirayama",
  "license": "MIT",
  "private": "true",
  "engines": {
    "node": ">=0.10.0"
  },
  "scripts": {
    "start": "npm run production",
    "production": "NODE_ENV=production npm run clean && npm run compile:scripts && npm run copy:files && npm run server",
    "development": "NODE_ENV=development npm run clean && npm run compile:scripts && npm run copy:files && (npm run compile:scripts:watch & npm run server:watch)",
    "server": "node dist/index.js",
    "server:watch": "nodemon --config .nodemonrc.json dist/index.js",
    "clean": "rm -rf dist",
    "copy:files": "cpx 'src/**/*.json' dist",
    "compile:scripts": "babel src --out-dir dist",
    "compile:scripts:watch": "babel src --out-dir dist -w --source-maps inline",
    "lint:scripts": "eslint --ignore-path .gitignore src",
    "lint:scripts:fix": "eslint --ignore-path .gitignore --fix src",
    "db:create": "mysql -u root < src/config/db.sql",
    "db:migrate": "sequelize db:migrate --config src/config/config.json",
    "migration:create": "sequelize migration:create --config src/config/config.json --name"
  },
  "dependencies": {
    "body-parser": "^1.16.0",
    "cheerio-httpcli": "^0.6.10",
    "express": "^4.14.0",
    "mysql": "^2.13.0",
    "normalize-url": "^1.9.0",
    "pug": "^2.0.0-beta9",
    "sequelize": "^3.30.0",
    "sequelize-cli": "^2.5.1"
  },
  "devDependencies": {
    "babel-cli": "^6.22.2",
    "babel-preset-es2015": "^6.22.0",
    "cpx": "^1.5.0",
    "eslint": "^3.14.1",
    "eslint-config-xo-space": "^0.15.0",
    "nodemon": "^1.11.0"
  }
}
